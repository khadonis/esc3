#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../template-html/bin/template-html" "$@"
  ret=$?
else 
  node  "$basedir/../template-html/bin/template-html" "$@"
  ret=$?
fi
exit $ret
